# Production Telegram Bot (Python & FastAPI)

An optimized async Telegram Bot using `python-telegram-bot` and `FastAPI`, pre-configured for direct zero-friction deployment on Render.

## 🚀 Local Quickstart

### 1. Install Dependencies
```bash
pip install -r requirements.txt
